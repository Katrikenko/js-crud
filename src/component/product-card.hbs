<div class="form__card">
	<div class="card__content">
		<h1 class="card__title">{{name}}</h1>
		<p class="card__description">{{description}}</p>
		<small class="card__id">ID: {{id}}</small>
		<div>
			<b class="card__title">{{price}}₴</b>
			<a class="card__link" href="/product-edit?id={{id}}">Редагувати</a>
		</div>
	</div>
</div>